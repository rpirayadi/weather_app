{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "swift-build",
            "type": "shell",
            "command": "swift build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Run weather_app",
            "type": "process",
            "command": "${workspaceFolder}/.build/debug/weather_app",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}